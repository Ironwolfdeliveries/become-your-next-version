import { JsonLd, PageHero } from "@/components/ui"; import { faqs } from "@/lib/data";
export const metadata={title:"Frequently Asked Questions",description:"Answers about BYNV, the Version Score, Kai, membership and privacy."};
export default function FAQ(){return <><JsonLd data={{"@context":"https://schema.org","@type":"FAQPage",mainEntity:faqs.map(([q,a])=>({"@type":"Question",name:q,acceptedAnswer:{"@type":"Answer",text:a}}))}}/><PageHero eyebrow="FAQ" title="Questions, answered clearly." copy="What Stage 1 is, what it is not, and what we intend to build next."/><div className="content container">{faqs.map(([q,a])=><details key={q} className="card"><summary><strong>{q}</strong></summary><p>{a}</p></details>)}</div></>}
