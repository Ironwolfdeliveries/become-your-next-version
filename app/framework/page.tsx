import { Button, CTA, PageHero } from "@/components/ui"; import { pillars } from "@/lib/data";
export const metadata={title:"The Architect Method",description:"A four-part cycle for deliberate personal growth."};
export default function Framework(){return <><PageHero eyebrow="The Architect Method" title="A blueprint for continuous becoming." copy="A simple, repeatable cycle: see clearly, design deliberately, build consistently and evolve continually."><Button href="/assessment">Find your starting point</Button></PageHero><section className="container cards">{pillars.map(p=><article className="card" key={p.number}><p className="eyebrow">Phase {p.number}</p><h2>{p.title}</h2><p>{p.text}</p></article>)}</section><div className="content container"><h2>Cycle, not finish line.</h2><p>The method does not promise permanent optimisation. Context changes. You change. Each cycle is an invitation to gather a more honest signal and adjust with care.</p><h2>From reflection to evidence.</h2><p>A useful plan specifies a behaviour you control, a lightweight rhythm and a review date. The goal is not a perfect streak; it is information you can use for the next iteration.</p></div><CTA /></>}
