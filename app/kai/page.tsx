import { KaiDemo } from "@/components/kai-demo"; import { PageHero } from "@/components/ui";
export const metadata={title:"Kai — BYNV Guide",description:"Understand how Kai uses approved BYNV context to support reflection and action."};
export default function Kai(){const live=Boolean(process.env.OPENAI_API_KEY);return <><PageHero eyebrow="Meet Kai" title="Your guide through the BYNV system." copy="Kai connects the page you are viewing with the private BYNV context you have chosen to save, then helps identify a useful next action."/><div className="container"><KaiDemo live={live} /></div><div className="content container"><h2>Private context with clear boundaries.</h2><p>Kai can use your Snapshot, Architect Assessment, Blueprint, Daily Focus, goals, cycles and challenges after the secure model connection is activated. Journal entries are excluded unless BYNV later adds an explicit opt-in.</p><p>Kai does not replace a therapist, clinician, crisis service or qualified professional adviser. During either assessment, Kai may clarify a question but will never recommend an answer.</p></div></>}
