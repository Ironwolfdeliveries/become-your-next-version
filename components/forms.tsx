"use client";
import { useState } from "react";
import type { FormEvent } from "react";
import { mockServices } from "@/lib/services";
export function InterestForm() { const [message, setMessage] = useState(""); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setMessage("Saving…"); const fd = new FormData(e.currentTarget); const r = await mockServices.submitInterest(String(fd.get("email"))); setMessage(r.message); } return <form className="form" onSubmit={submit}><label>Email address<input required name="email" type="email" autoComplete="email" placeholder="you@example.com" /></label><label className="check"><input required type="checkbox" /> I agree to receive BYNV early-access updates and can unsubscribe at any time.</label><button className="button">Request early access →</button><p role="status">{message}</p></form> }
export function ContactForm() { const [message, setMessage] = useState(""); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setMessage("Sending…"); const r = await mockServices.sendContact(); setMessage(r.message); } return <form className="form" onSubmit={submit}><label>Name<input required name="name" autoComplete="name" /></label><label>Email<input required name="email" type="email" autoComplete="email" /></label><label>Topic<select name="topic"><option>General question</option><option>Membership</option><option>Partnership</option><option>Privacy</option></select></label><label>Message<textarea required name="message" rows={6} /></label><button className="button">Send message →</button><p role="status">{message}</p></form> }
