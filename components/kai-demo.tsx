import { Button } from "./ui";
import { KaiAvatar } from "./kai-avatar";
export function KaiDemo({ live }: { live: boolean }) { return <section className="chat kai-status-card"><div className="chat-head"><KaiAvatar /><div><strong>Kai</strong><small>BYNV guide + AI leverage architect</small></div></div><div className="messages"><div className="message"><small>Kai</small><p>Your saved Snapshot, Architect Assessment, Blueprint, Daily Focus, goals, cycles, and challenges can help me connect this page to a useful next action—and identify repeatable work AI can help carry.</p></div><div className="message"><small>{live ? "Available now" : "Activation status"}</small><p>{live ? "Open Ask Kai on any page, or build a portable handoff in the Daily OS for the AI assistant you prefer." : "The secure Kai service awaits its production model credential. The Daily OS AI Delegation Audit still teaches a transferable workflow without pretending a scripted response is live coaching."}</p></div><Button href={live ? "/daily-focus" : "/membership"}>{live ? "Open my Daily OS" : "See membership"}</Button></div><p className="fine-print">Kai is not professional or crisis support. Assessment guidance never recommends a response.</p></section>; }
