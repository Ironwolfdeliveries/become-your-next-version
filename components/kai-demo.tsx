import { Button } from "./ui";
import { KaiAvatar } from "./kai-avatar";
export function KaiDemo({ live }: { live: boolean }) { return <section className="chat kai-status-card"><div className="chat-head"><KaiAvatar /><div><strong>Kai</strong><small>BYNV coaching guide</small></div></div><div className="messages"><div className="message"><small>Kai</small><p>Your saved Snapshot, Architect Assessment, Blueprint, Daily Focus, goals, cycles, and challenges can help me connect this page to a useful next action.</p></div><div className="message"><small>{live ? "Available now" : "Activation status"}</small><p>{live ? "Open Ask Kai on any page to begin a private, page-aware conversation." : "The secure Kai service is built but awaits its production model credential. BYNV will not present scripted text as a live personal response."}</p></div><Button href={live ? "/dashboard" : "/membership"}>{live ? "Open my dashboard" : "See membership"}</Button></div><p className="fine-print">Kai is not professional or crisis support. Assessment guidance never recommends a response.</p></section>; }
